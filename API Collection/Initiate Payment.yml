info:
  name: Initiate Payment
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/payments/initiate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
